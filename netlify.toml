[build]
  command = "npm run build"
  publish = "dist"

# 禁用构建缓存以确保最新代码生效
[build.environment]
  NODE_VERSION = "18"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

# 禁用浏览器和CDN缓存
[[headers]]
  for = "/*"
  [headers.values]
    Cache-Control = "no-cache, no-store, must-revalidate"
