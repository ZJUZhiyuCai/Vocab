# Design Token 设计系统
# VocabContext 语境词汇学习工具

## 一、设计风格定位

**风格关键词**：
- **高级 (Premium)**：精致的视觉细节，动态环境光效
- **稳健 (Robust)**：高对比度色彩，全浏览器兼容性优先
- **极简 (Minimalist)**：减少视觉干扰，聚焦沉浸式学习

**配色方案**：Premium 翡翠绿配色
- **翡翠绿 (Emerald Green)** - 核心品牌色 & 主交互色 (Emerald-600)
- **板岩灰/深蓝 (Slate)** - 基础架构色
- **动态背景 (Dynamic Background)** - 深色模式（高级渐变）/ 浅色模式（高亮洁净）

**圆角风格**：2XL 圆角 (16px) - 现代高级感与交互反馈优先

---

## 二、Color Palette - Premium 翡翠绿配色

### 主色调 - 翡翠绿 (Emerald)

```css
--emerald-50:   #ecfdf5   /* 极浅色 - 选中高亮 */
--emerald-100:  #d1fae5   /* 浅色 - 边框/背景 */
--emerald-500:  #10b981   /* 品牌色 - 基础 */
--emerald-600:  #059669   /* 核心品牌色 - 按钮/Logo/高对比度区域 (主要使用) */
--emerald-700:  #047857   /* 深色 - 悬浮态 */
```

> [!IMPORTANT]
> 为了确保在所有浏览器（包括部分渐变渲染异常的浏览器）中文字的可见性，核心按钮与 Logo 统一使用 **Emerald-600** 固态背景，确保与白色文字有足够的对比度。

### 辅助色 - 石板蓝

```css
--secondary-50:  #f1f3f5   /* 极浅色 */
--secondary-100: #e3e7eb   /* 浅色 */
--secondary-200: #c5d1da   /* 中浅色 */
--secondary-300: #9badbd   /* 常用浅色 - 信息提示 */
--secondary-400: #73889c   /* 中色 */
--secondary-500: #52667c   /* 次要色 */
```

### 背景色 - 柔和米棕

```css
--neutral-50:   #faf8f6   /* 页面背景 */
--neutral-100:  #f5f0eb   /* 卡片背景 */
--neutral-200:  #e8e0d8   /* 分隔线 */
--neutral-300:  #d4c4b0   /* 边框 */
--neutral-400:  #b8a68a   /* 次要文字 */
--neutral-500:  #9a8b72   /* 深色文字 */
```

### 功能色 - 成功/错误/警告

```css
/* 成功 - 答对时 */
--success-bg:      #e0ebe0   /* 浅绿背景 */
--success-text:    #5c7d5c   /* 绿色文字 */
--success-border:  #7d9c7d   /* 绿色边框 */

/* 错误 - 答错时 */
--error-bg:        #f0e6e6   /* 浅红背景 */
--error-text:      #b86c6c   /* 红色文字 */
--error-border:    #c98d8d   /* 红色边框 */

/* 警告 - 复习提醒 */
--warning-bg:      #f8f2e6   /* 浅黄背景 */
--warning-text:    #a68b5c   /* 黄色文字 */
--warning-border:  #b89d6b   /* 黄色边框 */
```

### 文字色阶

```css
--text-primary:   #3d3d3d   /* 主要文字 */
--text-secondary: #6b6b6b   /* 次要文字 */
--text-tertiary:  #9a9a9a   /* 辅助文字 */
--text-disabled:  #c5c5c5   /* 禁用文字 */
```

---

## 三、Typography 字体系统

### 字体栈

```css
/* 英文字体 */
--font-family-en: -apple-system, BlinkMacSystemFont, 'Inter', 'Segoe UI', sans-serif;

/* 中文字体（如果有） */
--font-family-zh: -apple-system, BlinkMacSystemFont, 'PingFang SC', 'Hiragino Sans GB', sans-serif;

/* 等宽字体（音标、代码） */
--font-family-mono: 'SF Mono', 'Monaco', 'Courier New', monospace;
```

### 字号系统

```css
/* 单词本体 - 最重要 */
--font-size-word:       28px;  /* 移动端 24px */
--font-weight-word:     700;

/* 例句文本 */
--font-size-sentence:   15px;
--font-weight-sentence: 400;
--line-height-sentence: 1.7;   /* 长文本易读 */

/* 页面标题 */
--font-size-h1:         24px;
--font-weight-h1:       600;

/* 小标签/提示 */
--font-size-caption:    13px;
--font-weight-caption:  500;

/* 音标 */
--font-size-ipa:        14px;
--font-weight-ipa:      400;
```

---

## 四、Spacing 间距系统

**基础单位**：4px（所有间距是4的倍数）

```css
--space-1:  4px   /* 标签内边距 */
--space-2:  8px   /* 紧密元素 */
--space-3:  12px  /* 相关元素 */
--space-4:  16px  /* 卡片内边距 */
--space-5:  20px  /* 重要间距 */
--space-6:  24px  /* 卡片间距 */
--space-8:  32px  /* 区块间距 */
--space-10: 40px  /* 页面边距 */
```

### 使用原则

- 碎片化快速刷词：信息紧凑但不拥挤
- 卡片内边距：16-20px
- 卡片间距：16-24px
- 保持视觉呼吸感

---

## 五、Border & Radius 边框与圆角

### 圆角系统（小圆角 - 专业感）

```css
--radius-sm:   4px;   /* 标签、按钮 */
--radius-md:   6px;   /* 输入框 */
--radius-lg:   8px;   /* 卡片 */
--radius-full: 9999px; /* 胶囊形状 */
```

### 边框系统

```css
--border-light:  1px solid var(--neutral-200);  /* 分隔线 */
--border-medium: 1px solid var(--neutral-300);  /* 卡片边框 */
--border-focus:  2px solid var(--primary-400);  /* 聚焦状态 */
```

---

## 六、Shadow 阴影系统

```css
/* 卡片默认 */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.04);

/* 卡片悬浮 */
--shadow-md: 0 2px 8px rgba(0, 0, 0, 0.08);

/* 弹窗 */
--shadow-lg: 0 8px 16px rgba(0, 0, 0, 0.10);

/* 聚焦/激活 - 主色光晕 */
--shadow-focus: 0 0 0 3px rgba(92, 107, 92, 0.3);
```

### 阴影原则

- 轻阴影，不过度强调
- 保持视觉层次
- 聚焦时用主色光晕

---

## 七、Animation 动画系统

### 时长与缓动

```css
--duration-fast:   150ms;  /* 反馈动画 */
--duration-base:   250ms;  /* 过渡动画 */
--duration-slow:   400ms;  /* 复杂动画 */

--ease-out: cubic-bezier(0.33, 1, 0.68, 1);    /* 快速退出 */
--ease-in-out: cubic-bezier(0.65, 0, 0.35, 1); /* 平滑 */
```

### 核心动画

#### 1. 卡片切换 - 淡入淡出

```css
@keyframes fadeIn {
  from {
    opacity: 0;
    transform: translateY(8px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
/* 应用：animation: fadeIn 250ms var(--ease-out) */
```

#### 2. 答对 - 成功闪烁 + 彩带

```css
/* 进度条跳动 */
@keyframes progressBump {
  0% { transform: scaleY(1); }
  50% { transform: scaleY(1.3); }
  100% { transform: scaleY(1); }
}

/* 绿色脉冲边框 */
@keyframes successPulse {
  0% { box-shadow: 0 0 0 0 rgba(140, 175, 156, 0.4); }
  50% { box-shadow: 0 0 0 8px rgba(140, 175, 156, 0); }
  100% { box-shadow: 0 0 0 0 rgba(140, 175, 156, 0); }
}

/* 彩带飘落 */
@keyframes confettiFall {
  0% {
    transform: translateY(-100%) rotate(0deg);
    opacity: 1;
  }
  100% {
    transform: translateY(100vh) rotate(720deg);
    opacity: 0;
  }
}
```

#### 3. 答错 - 晃动反馈

```css
@keyframes shake {
  0%, 100% { transform: translateX(0); }
  20%, 60% { transform: translateX(-4px); }
  40%, 80% { transform: translateX(4px); }
}
/* 应用：animation: shake 200ms var(--ease-in-out) */
```

#### 4. 进度条增长

```css
@keyframes progressGrow {
  from { width: 0; }
  to { width: var(--target-width); }
}
/* 应用：transition: width 250ms var(--ease-out) */
```

---

## 八、核心组件示例

### 单词卡片

```
┌─────────────────────────────────────┐
│  comprehensive                      │  ← 28px, 加粗, 鼠尾绿
│  /kəmˈprehensɪv/  adj.             │  ← 13px, 灰色, 音标
│                                     │
│  The government's **comprehensive** │
│  strategy addresses both economic   │
│  and environmental concerns.        │  ← 15px, 行高1.7
│                                     │
│  🤖 AI例句:                         │
│  This project needs a ______ test   │
│  plan before we launch.             │
└─────────────────────────────────────┘
   背景：#f5f0eb（米棕）
   圆角：8px
   阴影：0 1px 2px rgba(0,0,0,0.04)
```

### 答对反馈卡片

```
┌─────────────────────────────────────┐
│  ✓ 正确！                            │  ← 背景 #e0ebe0（浅绿）
│                                     │
│  comprehensive = 全面的，综合的      │
│  常见搭配：comprehensive study       │
│             comprehensive plan       │
└─────────────────────────────────────┘
```

### 答错反馈卡片

```
┌─────────────────────────────────────┐
│  ✗ 不太对                            │  ← 背景 #f0e6e6（浅红）
│                                     │
│  正确答案：comprehensive             │
│                                     │
│  💡 记忆技巧：                       │
│  com(一起) + prehen(抓住) + sive    │
│  = 全部抓住 → 全面的                 │
└─────────────────────────────────────┘
   晃动动画：200ms
```

---

## 九、Tailwind CSS 配置

```javascript
// tailwind.config.js
export default {
  theme: {
    extend: {
      colors: {
        primary: {
          50: '#f0f4f0',
          100: '#dce8dc',
          200: '#c0d5c0',
          300: '#9caf9c',
          400: '#7d8f7d',
          500: '#5c6b5c',
        },
        secondary: {
          50: '#f1f3f5',
          100: '#e3e7eb',
          200: '#c5d1da',
          300: '#9badbd',
          400: '#73889c',
          500: '#52667c',
        },
        neutral: {
          50: '#faf8f6',
          100: '#f5f0eb',
          200: '#e8e0d8',
          300: '#d4c4b0',
          400: '#b8a68a',
          500: '#9a8b72',
        },
        success: {
          bg: '#e0ebe0',
          text: '#5c7d5c',
          border: '#7d9c7d',
        },
        error: {
          bg: '#f0e6e6',
          text: '#b86c6c',
          border: '#c98d8d',
        }
      },
      fontFamily: {
        sans: ['Inter', 'system-ui', 'sans-serif'],
        mono: ['SF Mono', 'Monaco', 'Courier New', 'monospace'],
      },
      fontSize: {
        'word': ['28px', { lineHeight: '1.2', fontWeight: '700' }],
        'sentence': ['15px', { lineHeight: '1.7', fontWeight: '400' }],
      },
      borderRadius: {
        'sm': '4px',
        'md': '6px',
        'lg': '8px',
      },
      boxShadow: {
        'sm': '0 1px 2px rgba(0, 0, 0, 0.04)',
        'md': '0 2px 8px rgba(0, 0, 0, 0.08)',
        'lg': '0 8px 16px rgba(0, 0, 0, 0.10)',
        'focus': '0 0 0 3px rgba(92, 107, 92, 0.3)',
      }
    }
  }
}
```

---

## 十、移动端适配

```css
/* 移动端特定调整 */
@media (max-width: 768px) {
  :root {
    --font-size-word: 24px;      /* 单词稍小 */
    --space-4: 12px;             /* 卡片内边距减小 */
    --space-6: 16px;             /* 卡片间距减小 */
  }

  /* 触摸友好的点击区域 */
  .touch-target {
    min-height: 44px;
    min-width: 44px;
  }
}
```

---

## 十一、无障碍设计

### 对比度

所有文字与背景对比度符合 WCAG AA 标准：
- 主要文字：至少 4.5:1
- 大号文字（18px+）：至少 3:1

### 焦点状态

所有交互元素都有明确的焦点指示：
```css
:focus-visible {
  outline: 2px solid var(--primary-500);
  outline-offset: 2px;
}
```

---

## 十二、多主题系统 (Theme System)

应用采用一套动态响应的主题系统，通过 `useTheme` 组合式函数管理。

### 1. 深色模式 (Dark Mode) - 默认高级感
- **背景**: `bg-gradient-to-br from-slate-950 via-slate-900 to-gray-900`
- **装饰**: 包含三级动态模糊光晕（Emerald, Violet, Amber）及网格纹理。
- **文字**: Slate-100 (主要) / Slate-400 (次要)。

### 2. 浅色模式 (Light Mode) - 清洁高效
- **背景**: `bg-gray-100` (中性回退) 或配合微妙渐变。
- **装饰**: 为确保文字清晰度，浅色模式下弱化了环境光效，保持容器背景高白度。
- **文字**: Slate-900 (主要) / Slate-600 (次要)。

---

**文档版本**：v1.6
**最后更新**：2026-01-18
**设计原则**：高级、稳健、极简
